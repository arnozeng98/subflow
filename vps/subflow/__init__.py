"""
subflow 包。

本包承载 VPS 侧的私有 API，与内置的 sing-box 管理器（以 vendor 形式置于
vps/singbox 下）并列运行。代码刻意拆分为若干职责单一的小模块，因为本项目同时
面临两个相互制衡的约束：

1. 必须能在 VPS 上用一条命令轻松完成安装。
2. 在支持多种客户端订阅格式与多个上游数据源的前提下，必须保持可维护性。

保持代码模块化是同时满足这两个约束的唯一办法，否则私有 API 会重新退化成一个
庞大的单体脚本。
"""
